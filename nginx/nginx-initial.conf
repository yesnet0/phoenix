events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name phoenix.disclose.io;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 200 'Phoenix is being configured...';
            add_header Content-Type text/plain;
        }
    }
}
